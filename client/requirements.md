## Packages
framer-motion | Smooth layout transitions and entry animations
date-fns | Formatting timestamps for intentions and messages
clsx | Conditional class merging
tailwind-merge | Class merging utility

## Notes
Tailwind config should extend fontFamily to include 'Playfair Display' for headings and 'Lato' for body.
App uses Replit Auth - login/logout flows are handled via /api/login and /api/logout.
